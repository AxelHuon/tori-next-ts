'use client';
import React from 'react';

interface LoginProps {
  params: any;
}

const Page: React.FC<LoginProps> = ({ params }) => {
  console.log(params);
  return <>errro</>;
};

export default Page;
